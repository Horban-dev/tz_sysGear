import React from 'react';
import Table from './sorting-components.js/Table';

const Sorting = () => {
    return (
        <div className="container-md">
            <Table/>
        </div>
    );
};

export default Sorting;